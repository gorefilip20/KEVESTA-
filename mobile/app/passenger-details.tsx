import { useState } from "react";
import { KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";
import { useLocalSearchParams, useRouter } from "expo-router"; import { Ionicons } from "@expo/vector-icons"; import { colors } from "@/constants/theme";
export default function PassengerDetailsScreen() { const router = useRouter(); const params = useLocalSearchParams<Record<string, string>>(); const [firstName, setFirstName] = useState(""); const [lastName, setLastName] = useState(""); const [email, setEmail] = useState(""); const [phone, setPhone] = useState(""); const [error, setError] = useState(""); function next() { if (!firstName.trim() || !lastName.trim() || !/^\S+@\S+\.\S+$/.test(email)) { setError("Enter the passenger name and a valid email."); return; } router.push({ pathname: "/checkout", params: { type: "flight", id: params.flightId, title: `${params.airline} ${params.flightNumber}: ${params.origin} → ${params.destination}`, origin: params.origin, destination: params.destination, departDate: params.departDate, cabinClass: "economy", passengers: "1", seatPrice: params.seatPrice, seat: params.seat, passengerName: `${firstName} ${lastName}`, passengerEmail: email, passengerPhone: phone } }); } return <KeyboardAvoidingView style={styles.page} behavior={Platform.OS === "ios" ? "padding" : undefined}><ScrollView contentContainerStyle={styles.content}><Pressable onPress={() => router.back()} style={styles.back}><Ionicons name="arrow-back" size={18} color={colors.purple} /><Text style={styles.backText}>Seat {params.seat}</Text></Pressable><Text style={styles.eyebrow}>PASSENGER DETAILS</Text><Text style={styles.title}>Who is flying?</Text><Text style={styles.copy}>{params.airline} {params.flightNumber} · {params.origin} → {params.destination}</Text><View style={styles.card}><Text style={styles.section}>Passenger 1</Text><Text style={styles.label}>First name</Text><TextInput testID="passenger-first-name" value={firstName} onChangeText={setFirstName} placeholder="Alex" placeholderTextColor={colors.muted} style={styles.input} /><Text style={styles.label}>Last name</Text><TextInput testID="passenger-last-name" value={lastName} onChangeText={setLastName} placeholder="Morgan" placeholderTextColor={colors.muted} style={styles.input} /><Text style={styles.label}>Email address</Text><TextInput testID="passenger-email" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" placeholder="alex@example.com" placeholderTextColor={colors.muted} style={styles.input} /><Text style={styles.label}>Phone number <Text style={styles.optional}>Optional</Text></Text><TextInput testID="passenger-phone" value={phone} onChangeText={setPhone} keyboardType="phone-pad" placeholder="+1 555 000 0000" placeholderTextColor={colors.muted} style={styles.input} />{error ? <Text style={styles.error}>{error}</Text> : null}</View><Pressable testID="passenger-next" onPress={next} style={({ pressed }) => [styles.button, pressed && { opacity: .75 }]}><Text style={styles.buttonText}>Review and pay</Text><Ionicons name="arrow-forward" size={17} color={colors.surface} /></Pressable></ScrollView></KeyboardAvoidingView> }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: colors.bg }, content: { padding: 22, paddingBottom: 35 }, back: { flexDirection: "row", alignItems: "center", gap: 7, marginBottom: 26 }, backText: { color: colors.purple, fontWeight: "800", fontSize: 12 }, eyebrow: { color: colors.terracotta, fontSize: 10, letterSpacing: 1.4, fontWeight: "800" }, title: { color: colors.ink, fontSize: 31, fontWeight: "800", marginTop: 8 }, copy: { color: colors.muted, fontSize: 13, marginTop: 6 }, card: { backgroundColor: colors.surface, borderRadius: 20, borderWidth: 1, borderColor: colors.border, padding: 17, marginTop: 24 }, section: { color: colors.ink, fontSize: 17, fontWeight: "800", marginBottom: 5 }, label: { color: colors.ink, fontSize: 12, fontWeight: "800", marginTop: 14, marginBottom: 6 }, optional: { color: colors.muted, fontWeight: "500" }, input: { height: 48, borderRadius: 13, borderWidth: 1, borderColor: colors.border, paddingHorizontal: 13, color: colors.ink, fontSize: 14 }, error: { color: "#A13E38", fontSize: 12, marginTop: 12 }, button: { minHeight: 52, flexDirection: "row", gap: 8, alignItems: "center", justifyContent: "center", backgroundColor: colors.purple, borderRadius: 15, marginTop: 14 }, buttonText: { color: colors.surface, fontWeight: "800", fontSize: 13 } });
