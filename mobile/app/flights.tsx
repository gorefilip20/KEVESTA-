import { useState } from "react";
import { KeyboardAvoidingView, Platform, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { useRouter } from "expo-router"; import { Ionicons } from "@expo/vector-icons"; import { colors } from "@/constants/theme";
const DEFAULT_DEPART_DATE = new Date(Date.now() + 14 * 86400000).toISOString().slice(0, 10);
export default function FlightsScreen() { const router = useRouter(); const [origin, setOrigin] = useState("JFK"); const [destination, setDestination] = useState("LHR"); const [date, setDate] = useState(DEFAULT_DEPART_DATE); return <KeyboardAvoidingView style={styles.page} behavior={Platform.OS === "ios" ? "padding" : undefined}><View style={styles.content}><Pressable onPress={() => router.back()} style={styles.back}><Ionicons name="arrow-back" size={18} color={colors.purple} /><Text style={styles.backText}>Back</Text></Pressable><Text style={styles.eyebrow}>FLIGHT BOOKING</Text><Text style={styles.title}>Find your{`\n`}next departure.</Text><Text style={styles.copy}>Search flights, choose your seat, and keep every detail in one calm flow.</Text><View style={styles.card}><Text style={styles.label}>From</Text><View style={styles.inputRow}><Ionicons name="airplane-outline" size={18} color={colors.purple} /><TextInput testID="flight-origin" value={origin} onChangeText={setOrigin} autoCapitalize="characters" style={styles.input} placeholder="JFK" placeholderTextColor={colors.muted} /></View><View style={styles.divider} /><Text style={styles.label}>To</Text><View style={styles.inputRow}><Ionicons name="location-outline" size={18} color={colors.purple} /><TextInput testID="flight-destination" value={destination} onChangeText={setDestination} autoCapitalize="characters" style={styles.input} placeholder="LHR" placeholderTextColor={colors.muted} /></View><View style={styles.divider} /><Text style={styles.label}>Departure date</Text><View style={styles.inputRow}><Ionicons name="calendar-outline" size={18} color={colors.purple} /><TextInput testID="flight-date" value={date} onChangeText={setDate} style={styles.input} placeholder="YYYY-MM-DD" placeholderTextColor={colors.muted} /></View></View><Pressable testID="flight-search-submit" onPress={() => router.push({ pathname: "/flight-results", params: { origin: origin.trim().toUpperCase(), destination: destination.trim().toUpperCase(), departDate: date } })} style={({ pressed }) => [styles.button, pressed && { opacity: .75 }]}><Text style={styles.buttonText}>Search flights</Text><Ionicons name="arrow-forward" size={18} color={colors.surface} /></Pressable></View></KeyboardAvoidingView> }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: colors.bg }, content: { padding: 22, paddingTop: 18 }, back: { flexDirection: "row", alignItems: "center", gap: 7, marginBottom: 28 }, backText: { color: colors.purple, fontWeight: "800", fontSize: 12 }, eyebrow: { color: colors.terracotta, fontSize: 10, letterSpacing: 1.4, fontWeight: "800" }, title: { color: colors.ink, fontSize: 32, lineHeight: 35, fontWeight: "800", marginTop: 8 }, copy: { color: colors.muted, fontSize: 14, lineHeight: 20, marginTop: 11 }, card: { marginTop: 25, padding: 17, backgroundColor: colors.surface, borderRadius: 20, borderWidth: 1, borderColor: colors.border }, label: { color: colors.muted, fontSize: 11, fontWeight: "800", marginBottom: 7 }, inputRow: { flexDirection: "row", alignItems: "center", gap: 10 }, input: { flex: 1, color: colors.ink, fontSize: 16, fontWeight: "800", paddingVertical: 4 }, divider: { height: 1, backgroundColor: colors.border, marginVertical: 15 }, button: { minHeight: 52, flexDirection: "row", gap: 9, alignItems: "center", justifyContent: "center", backgroundColor: colors.purple, borderRadius: 15, marginTop: 16 }, buttonText: { color: colors.surface, fontSize: 14, fontWeight: "800" } });
